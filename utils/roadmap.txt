
class 

  - constructor(hexadecimal:string)
    -- this.color = hexadecimal && hexadecimal || this.makeHexadecimal()

  - methods
    -- this.HexadecimalModifier()
       -- destructure
       -- modify
    -- this.HexadecimalValidator()
       -- toUpperCase()
       -- pure 6 digit (#a2c == #aa22cc)

    -- this.makeHexadecimal()
    -- this.makeHexaToRGB(color:string)
    -- this.makeHexaToHSL(color:string)
    -- this.makeHexaToRGBA(color:string)
    
    -- this.makeOppositeHexadecimal(color:string)
    -- this.makeOppositeRGB(color:string)
    -- this.makeOppositeHSL(color:string)
    -- this.makeOppositeRGBA(color:string)

    -- this.makeVariant(color:string)

    -- this.generateHexadecimal()
    -- this.generateRGB()
    -- this.generateHSL()
    -- this.generateRGBA()

    -- this.getColor()
    -- this.generateColors(limit:numbers)
    -- this.generateDarkColorVariant(limit:numbers)


[X] variant maker (universal/for all)
    -- HSL
[X] Dark Color variant
[x] White Color variant
[x] Color Mixer

